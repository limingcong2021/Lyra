{
  "name": "dol-lyra-signaling-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for DoL-Lyra PVP signaling server",
  "type": "module",
  "main": "signaling-server.js",
  "scripts": {
    "build": "echo 'No build needed for Worker'",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}